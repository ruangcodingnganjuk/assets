## MENGUBAH GAMBAR MENJADI TEKS DENGAN PYTHON

Library yang perlu diinstal:
1. Install pytesseract
    > pip install pytesseract
2. Install OpenCV/ pillow
    > pip install opencv-python
    > pip install pillow